# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Workflow

- Never commit directly to `main`. Before starting any work, create a new branch off `main` (branch off the latest `main`, not an existing feature branch).
- Before committing, always update documentation affected by the change — `CLAUDE.md` (Status, and whichever architecture section covers what changed), `README.md`, `API.md`, and `docs/SPEC03.md` if the change alters mandated behavior. Check each for now-stale claims (a feature described as "not built yet" that this change built, a route/shape that changed, a screen that didn't exist before) and fix them in the same commit as the code, not a follow-up. Do this as a deliberate last step prior to committing, not opportunistically while coding.
- When the work is done, commit the changes and push the branch to the remote.
- After pushing, generate a Markdown summary of the changes so the user can open the PR on GitHub manually — do not open the PR yourself. (This is manual for now; may be automated later.)
- **Any non-dev container build (i.e. a build meant to be published, not a local `localscore:local` verification build) MUST be tagged, pushed, and verified per "Publishing a release image" below** — never `podman push` an ad-hoc/untagged image or push only `latest`.

## Status

The product works end-to-end: an npm-workspaces monorepo (`server/` = Hono + better-sqlite3 API, `web/` = React + Vite frontend), the full interview catalog (docs/SPEC01.md §5.2), environment CRUD with answer-derivation, the initial DB migration, a working **scoring engine** (`POST /api/score`, `server/src/scoring/`), a real **frontend** (environments list → interview wizard → results screen with both a paste-a-vector flow and a CVE-lookup flow, plus a saved-vulnerabilities list, all wired to the API), **NVD CVE lookup** (`GET /api/cve/:cveId`, `server/src/lib/nvd.ts`), and **saved-vulnerability CRUD** (`server/src/routes/vulnerabilities.ts`). No remaining stubbed API routes and no remaining unbuilt UI surface for the routes above — see "Frontend" below for what's built and the one documented backend-shape gap it works around.

**docs/SPEC02.md v1.1 is fully implemented** — §2–§9 are all done, including §8.4 (interview question help modal, closed last) — see "Design system", "Markdown description fields", "Environments page", "Saved vulnerabilities", and "Scoring page" under Frontend below.

**docs/SPEC03.md (v1.2) is fully implemented** — §2–§8 are all done: the copy/branding pass (bold product name, no em-dashes anywhere user-facing including the interview catalog, green Save buttons), light-theme severity-pill legibility, the SPEC02 §2.1 primary-button/active-tab theme-inversion, real tab styling with per-tab purpose text, environments-page layout fixes, richer question-help modals (`helpDetail`), CVE detail enrichment, the inline more-vulnerable alert, and the Saved-page affordance fixes — see "SPEC03 UI polish, tabs, and CVE detail enrichment" under Frontend below.

**docs/SPEC04.md (v1.3) is fully implemented** — §2 (color-system refresh), §3 (Title Case button labels), §4 (environment risk warning), §5.1 (NVD-description prefill), and §5.2 (collapsed-by-default CVE details) are all done — see "SPEC04 color refresh and Title Case", "SPEC04 environment risk warning", and "SPEC04 NVD-description prefill and collapsed CVE details" under Frontend below.

**docs/SPEC05.md (v1.4) is fully implemented** — §2 (global chrome: theme-toggle switch, Edit-control redesign, layout-stable scrollbar), §3 (environments `location` field + two-tier risk warning), §4 (Scoring-page input focus + Major-CVE-ID typography), and §5 (Saved-page header-action wrapping, CVE-details disclosure grammar, de-blued detail chips, NVD link) are all done — see "SPEC05 global chrome", "SPEC05 location field and two-tier risk warning", "SPEC05 Scoring-page focus and typography", and "SPEC05 Saved-page fixes" under Frontend below.

**docs/SPEC06.md (v1.5) is in progress** — §2 (environments: quote-stripping, risk-callout readability) is done, landing in its own branch/PR ahead of the remaining Scoring-page (§3) and Saved-page (§4) work, which are separate in-flight branches — see "SPEC06 environment quote-stripping and risk-callout readability" under Frontend below.

### NVD lookup & saved vulnerabilities

- `server/src/lib/nvd.ts` — fetches `services.nvd.nist.gov`, throttled module-wide (~1 req/6s unauthenticated, faster with `NVD_API_KEY`) since NVD's unauthenticated rate limit is shared across all callers in the process, not per-request. `extractVectorOptions()` parses every `cvssMetricV40`/`V31`/`V30` entry NVD returns (deliberately skips `cvssMetricV2` — v2 isn't a supported score) so the UI can show all disagreeing sources per docs/SPEC01.md §7; `pickPrimaryVector()` picks the highest-version/Primary-sourced one as the default.
- `GET /api/cve/:cveId` (`server/src/routes/cve.ts`) is cache-first against the `vulnerabilities` table (same table doubles as the NVD cache and the saved-vulnerability list) — a cached CVE is served with no network call unless `?refresh=1`. A failed refresh falls back to serving the stale cache rather than erroring, per §7's offline behavior.
- The cache/saved-list dual use is disambiguated by a `saved INTEGER` column (migration `0002_vulnerabilities_saved_flag.sql`, per docs/SPEC02.md §7.1): lookups write/update rows with `saved = 0` and never flip an existing row's flag; `GET /api/vulnerabilities` only returns `saved = 1` rows. This closed a v1 bug where a lookup-then-save produced two list entries.
- `server/src/routes/vulnerabilities.ts` always re-parses and re-scores the vector server-side on save (`parseBaseVector` + `computeScore`) rather than trusting a client-supplied score — consistent with treating derived values as recomputed, not client state, elsewhere in the app. Saving is an **upsert**, not an insert (docs/SPEC02.md §6.6/§7.1): matched by `cve_id` for NVD-sourced saves or by normalized `vector` for pasted-vector saves, reusing an existing cache row (`saved = 0 → 1`) or an already-saved row in place rather than creating a duplicate. The response's `overwritten` flag distinguishes "this updated an already-saved entry" (`200`) from a first save, including one that reused a cache row (`201`).
- **§7.3 NVD carry-through turned out to already work as a side effect of the §7.1 upsert**, not a separate fix: the POST upsert's `UPDATE ... nvd_json = COALESCE(?, nvd_json)` means that when a save matches an existing cache row by `cve_id` (the normal CVE-lookup-then-save path), it keeps that row's already-populated `nvd_json` even though the client never sends one — the client genuinely never sends `nvdJson` today, and it doesn't need to. Confirmed by a real (non-mocked) NVD lookup during manual verification, then locked in with a mocked-fetch regression test (`server/test/vulnerabilities-route.test.ts`, "NVD data carries through on save"). The v1 "known gap" this was tracked against no longer applies.
- Tests mock global `fetch` via `vi.stubGlobal` (`server/test/cve-route.test.ts`); the throttle's module-level `lastRequestAt` needed a test-only reset hook (`__resetNvdThrottleForTests`) since otherwise it bleeds across unrelated test cases sharing the same process and causes spurious 5s+ timeouts.
- `GET /api/major-cves` (`server/src/routes/major-cves.ts`, docs/SPEC02.md §6.5) — a *distinct* path from `/api/cve/:cveId`, deliberately not routed through it. `fetchMajorCves()` (`nvd.ts`) runs two separate NVD searches (`cvssV3Severity=CRITICAL` and `cvssV4Severity=CRITICAL`, both over a 30-day `pubStartDate`/`pubEndDate` window) since NVD's search API has no OR-across-severity-fields query — results are merged by `cveId` (deduping a CVE that's critical under both v3 and v4, preferring the v4.0 entry via the existing `pickPrimaryVector()`), sorted by score desc / most-recent-published as tiebreak, and clamped to 10. Cached in a new single-row `major_cves_cache` table (migration `0003_major_cves_cache.sql`) with a `fetched_at`; the route serves the cache unconditionally while it's <24h old, refreshes lazily on access when stale, and falls back to serving the stale cache (with its `fetched_at` disclosed) if the refresh fails — same offline philosophy as the per-CVE cache. No scheduler; refresh is purely access-driven.
- Because it makes two throttled NVD calls per request (each serialized ~6s apart unauthenticated, per the shared module-level throttle), `server/test/major-cves-route.test.ts`'s tests that exercise the real fetch path take ~6s each — `server/vitest.config.ts` sets `testTimeout: 15000` project-wide to cover this (the default 5s isn't enough).

### Frontend

`web/src/` — `App.tsx` does simple state-based view switching (`environments` / `environment-edit` / `interview` / `score` / `vulnerabilities`), no router library. `pages/` holds the five screens, `components/` the shared pieces (`SeverityPill`, `AnimatedScore`, `ScoreChanges`, `EnvironmentResultRow`, `ScoreResult`, `NvdVectorPicker`, `Icon`, `Modal`, `ThemeToggle`, `MarkdownContent`, `MoreExpander`, `WarningBanner`). `api.ts` wraps every `/api/*` call; `types.ts` mirrors the server's response shapes.

- `ScoreResult.tsx` (extracted from what used to be inline in `ScorePage.tsx`) renders the base-score-card + per-environment result list, and optionally an inline-expand "Save" action (`onSave` prop) that POSTs to `/api/vulnerabilities`. Omitting `onSave` hides the control — used when viewing an already-saved item from `SavedVulnerabilitiesPage.tsx`. Its optional `overwriteLabel` prop (see "Scoring page" below) shows an italic note in the save panel when the save will update an existing entry rather than create one, per docs/SPEC02.md §6.6.
- `SavedVulnerabilitiesPage.tsx` lists saved vulnerabilities (mirrors `EnvironmentsPage.tsx`'s list/empty-state/delete structure) with inline expand-to-view-and-rescore per row via `ScoreResult`. Each `<li>` is one `.card.vulnerability-row` containing both the clickable header and the conditionally-rendered detail panel (reuses `.result-row-detail`'s divider treatment from `EnvironmentResultRow.tsx`) — not a header card with a separate unstyled sibling — so the expanded content stays visibly bounded within its own card. Clicking anywhere on the header row toggles expand/collapse (`onClick` on `.vulnerability-row-header`); the View/Hide and Delete buttons inside it call `event.stopPropagation()` so they don't also re-trigger the row's own toggle. `SeverityPill` takes an optional `variant?: "solid" | "outline"` (default `"solid"`) — this page uses `"outline"` (border + text colored by severity, transparent fill) instead of the usual solid pill, since a solid pill here would carry the same visual weight as the neutral `.badge` tags next to it. The outline variant's colors are separate `--severity-*-text` tokens (root + dark-mode override, alongside the existing `--severity-*` fill tokens), not the raw fill hues — validated via the dataviz skill's `validate_palette.js` plus WCAG contrast math that e.g. `--severity-medium` measures only 1.79:1 as raw text against the light surface (fill-with-near-black-text and text-on-surface are different contrast pairings, and the fill tokens were only ever validated for the former).
- `web/src/lib/severity.ts` — `nvdSeverityToAppSeverity(raw, baseScore)` maps NVD's upper-case `baseSeverity` strings (or the score-band fallback when NVD omits one) onto the app's title-case `Severity` union; also reused to score-derive a severity pill for the saved-vulnerabilities list, which doesn't carry `baseSeverity` at all.

#### Markdown description fields (docs/SPEC02.md §4)

- `web/src/lib/markdown.ts` — `renderMarkdown(source)` runs `marked` (GFM, `breaks: true`) then sanitizes the output HTML through `dompurify` (an `afterSanitizeAttributes` hook forces `target="_blank"`/`rel="noopener noreferrer"` on every link; `<style>` is additionally forbidden). This is the only place raw description text becomes HTML — descriptions are stored as-is server-side (plain `TEXT` columns, no server-side sanitization) and sanitized only at render time, so raw markdown source round-trips intact through edit forms.
- `web/src/components/MarkdownContent.tsx` wraps `renderMarkdown` in a `<div className="markdown">` via `dangerouslySetInnerHTML` — this is the only `dangerouslySetInnerHTML` call in the app and is safe specifically because it always goes through the sanitizer first, never raw source.
- Wired up in `EnvironmentsPage.tsx`'s list rows (`env.description`) and `EnvironmentEditPage.tsx`'s detail rendering context (edited as a plain `.textarea`, per §4's "no WYSIWYG" rule). Saved-vulnerabilities have no `description` column yet (§7.2) — that will reuse `MarkdownContent` once built, not a new renderer.
- `web/src/lib/markdown.test.ts` is the §11.6-mandated safety test (Vitest + `jsdom` environment, see `web/vitest.config.ts`) — covers basic formatting, forced link attributes, and stripping of `<script>`, inline event-handler attributes, `javascript:` URIs, and `<style>`. This is the first web-side Vitest suite in the repo; `npm test` at the root now runs both `server` and `web` workspaces (previously server-only).

Design system lives in `web/src/styles.css` as CSS custom properties (no framework, per docs/SPEC01.md §3): IBM Plex Mono for headlines/scores/vector strings, Archivo for body copy (via `@fontsource/*`, self-hosted — **never** load fonts from a CDN, the spec requires the app work fully offline; docs/SPEC02.md reaffirms the no-CDN rule). Import only the `latin-*.css` subset files from `@fontsource` packages, not the bare weight files — the latter pull in every unicode subset (cyrillic, vietnamese, greek, ...) and roughly quadruple the font payload for no reason in an English-only UI.

#### Design system: semantic actions, icons, modal, theming (docs/SPEC02.md §2/§3)

- `Icon.tsx` — a single component (`name` prop) rendering bundled inline SVGs (`trash`, `pencil`, `sun`, `moon`, `chevron`, `question`, `warning`), no icon font/CDN per §2.3. Icon-only buttons always carry `aria-label`.
- Semantic action colors are new tokens in `styles.css`, deliberately distinct hues from the severity palette (which stays "reserved, never reused") — `--action-green`/`--action-yellow` (solid fills, near-black `--action-ink` text on top, same pairing pattern as the severity pills) and `--action-red-text`/`--action-yellow-text` (theme-varying outline/icon colors, same pattern as `--severity-*-text`). Contrast-validated in both themes via the dataviz skill's `validate_palette.js` `contrast()` export against the real `--surface`/`--surface-card` values, not eyeballed — e.g. `--action-green` clears 4.76:1 (light) / 8.36:1 (dark) with black text, `--action-red-text` clears 5.03:1 (light) / 5.83:1 (dark) as outline/text against `--surface`.
- Button classes: `.button-save` (solid green, "Save", never an icon) and `.button-cancel` (solid yellow, "Cancel", never an icon) share `.button`'s size system. `.icon-button` is a fixed-size (2.25rem) square icon-only control; `.icon-button-delete` (red outline + `Icon name="trash"`) and `.icon-button-edit` (yellow outline + `Icon name="pencil"`) are its semantic variants, `.icon-button-quiet` is the neutral one (used by `ThemeToggle`). `ScoreResult.tsx`'s save-confirm flow and both list pages' delete buttons are migrated onto these; the non-semantic buttons (`Next`, `Score it`, `Create & start interview`, etc.) intentionally stay on plain `.button`/`.button-primary` per §2.1 — semantic colors are reserved for the four actions in §2.2's table, not a general recolor.
- `Modal.tsx` exports `Modal` (generic dialog: blurred backdrop via `backdrop-filter`, `role="dialog"`/`aria-modal`, focus-trapped, closes on Escape or backdrop click, restores focus to the trigger on close) and `ConfirmModal` (the delete-confirmation specialization — yellow Cancel / red-outline-text "Delete" per §2.2). `EnvironmentsPage.tsx` and `SavedVulnerabilitiesPage.tsx` no longer use `window.confirm()`.
- Theming: `web/src/lib/theme.ts` reads/writes `localStorage` (`localscore-theme`) and toggles a `data-theme="light"|"dark"` attribute on `<html>`. `main.tsx` applies the preferred theme (stored value, else `prefers-color-scheme`) *before* the first React render to avoid a flash of the wrong theme. `styles.css` now defines dark tokens three times by design — behind `@media (prefers-color-scheme: dark)` (pre-JS default), and behind `:root[data-theme="dark"]`/`:root[data-theme="light"]` (both higher-specificity than a bare `:root`, so the in-app toggle always wins over system preference) — keep all three in sync when editing dark-mode tokens. `ThemeToggle.tsx` sits at the far right of the header (`.nav` has `margin-right: auto` to push it there) and shows the icon for the *action* (moon while light, sun while dark), not the current state.
- `.badge`/`.badge-severity` were audited against §2.5 and already share one box model (shape/size/weight), differing only in color — no change was needed there.

#### Environments page (docs/SPEC02.md §8)

- `EnvironmentsPage.tsx`'s page description was rewritten for a non-technical reader and gained a `MoreExpander` (§8.1) explaining the base-score-vs-environmental-metrics distinction. `MoreExpander.tsx` is a thin `<details>`/`<summary>` wrapper — the same shared component §6.1 reuses for the Scoring page's "More" — deliberately styled larger/more prominent than the interview's question-help-modal treatment (§8.4) for the same kind of content (different information density: page-level explainer vs. per-question disclosure).
- The create form now also collects an optional description (`.textarea`), closing v1 gap #2 (§10) — `POST /api/environments` already accepted one, the form just never sent it.
- **`EnvironmentEditPage.tsx`** (new) is the dedicated edit view mandated by §8.3, closing v1 gap #1: rename (`.input`), description (`.textarea`, markdown-rendered elsewhere per §4), a red-outline trash `icon-button-delete` (guarded by `ConfirmModal`), and green `.button-save`/yellow `.button-cancel` calling the now-added `updateEnvironment()` (`api.ts`) against `PUT /api/environments/:id` (already existed server-side, per docs/SPEC01.md v1 — only the UI was missing). A second card in the same view surfaces "Answer interview" / "Re-answer interview" (labeled by `interviewCompletion["3.1"]`) to reach the wizard, per §8.3's "interview reachable from this view."
- `EnvironmentsPage.tsx`'s list-row action follows §8.2's swap: once `interviewCompletion["3.1"]` is true the row shows the yellow pencil `icon-button-edit`; until then it shows a text "Answer interview" button. **Both now open `EnvironmentEditPage`, never the interview wizard directly** — `onOpenEdit` replaces the old `onOpenInterview` wiring on this page's row action (the Create form's "Create & start interview" still jumps straight into the wizard on creation, since there's nothing to edit yet).
- `InterviewPage.tsx`'s "Save & exit" `.button-quiet` control is gone per §8.3 ("replaces the interview's 'Save & exit' control... in favor of normal navigation out of the wizard") — replaced with a plain `.link-button` "← Back to environment" above the `<h1>`. Answers still auto-save per-answer as before. `App.tsx` routes the interview's `onDone` (both the back-link and the final "Done" button) back to `EnvironmentEditPage` for that environment, not the full environments list, since §8.3 frames the wizard as reached *from* the edit view.
- **§8.4 question help modal**: `whyWeAsk` and `finePrint` no longer render as permanent inline text — a circled `?` `icon-button-quiet` next to the question title (`QuestionHelpModal.tsx`) opens the shared `Modal` component (the same one delete confirmations use) with both. Closes on Escape, the backdrop, or an explicit "Close" button, blurs the page behind it, and never navigates away — `helpOpen` state resets on question change (an effect keyed on `index`) so switching questions via the progress ticks doesn't leave a stale modal open for the wrong question.

#### Saved vulnerabilities (docs/SPEC02.md §7.2–§7.4)

- `vulnerabilities.description TEXT NOT NULL DEFAULT ''` already existed as a column (added alongside `saved` in migration `0002_vulnerabilities_saved_flag.sql`, ahead of any route/UI using it) — §7.2 was purely wiring it up: `serializeVulnerability()` now includes it, and a new `PUT /api/vulnerabilities/:id` (`server/src/routes/vulnerabilities.ts`) accepts `{ label?, description? }` only — vector/score/cve_id stay fixed, matching the environments-edit route's COALESCE-on-missing-field pattern.
- `SavedVulnerabilitiesPage.tsx` gained a yellow pencil `icon-button-edit` per row (next to View/Delete) that swaps the row's header for an inline edit form (`.environment-edit-form`, the same class `EnvironmentEditPage.tsx` uses) with label/description fields and green `.button-save`/yellow `.button-cancel`, calling the new `updateVulnerability()` (`api.ts`). No separate edit page — the row itself becomes the form, then reverts to the normal header on save/cancel. The list also now renders each row's description via `MarkdownContent`, same as the environments list.
- §7.3's "NVD carry-through" fix turned out to be free — see the note on `vulnerabilities.ts` in "NVD lookup & saved vulnerabilities" above; the only work here was adding the regression test.
- `EnvironmentResultRow.tsx`'s disclosure chevron is now `<Icon name="chevron" size={20} />` (was a raw `▾` character) inside the same `.disclosure`/`.disclosure.is-open` rotating wrapper. `.delta` (the row-level score change) and `.why-impact` (the per-metric change lines in `ScoreChanges.tsx`) are both larger/bolder now, and both use the `--severity-*-text` tokens instead of the raw `--severity-low`/`--severity-critical` fill hues — the fill tokens fail contrast used as foreground text (same issue documented for the saved-vulnerabilities outline pills above). `ScoreChanges.tsx`'s "Total:" line gets a `why-total-worse`/`why-total-better` class (bold, `--severity-critical-text`/`--severity-low-text`) derived from the total's sign; zero stays the existing neutral style.
- New `.more-vulnerable-warning` callout (`EnvironmentResultRow.tsx`, shown inside the expanded detail panel whenever `result.delta > 0`) states plainly that the environment is more exposed than the base score suggests. It's a thin modifier (just spacing) on a new shared `.callout-warning` base class — a red-accented card + `Icon name="warning"`, backed by a new `--warning-wash` token (a translucent tint of `--action-red-text`, same pattern as `--accent-wash`; no separate contrast validation needed since body text stays `--ink` on a background that's still effectively `--surface-card` at that opacity). `.callout-warning` is also what backs the new `WarningBanner.tsx` component used by the Scoring page's CVE-lookup errors (§6.3) — one visual language for "something's wrong, here's why" everywhere in the app. Because both the Scoring-page results and the saved-vulnerability detail view render through the same `EnvironmentResultRow`, one implementation covers both places §7.4 requires the more-vulnerable warning.

#### Scoring page (docs/SPEC02.md §6)

- `ScorePage.tsx` was rewritten around three tabs (`role="tablist"`/`tab`/`tabpanel`; visual treatment now `.tabs`/`.tab` per docs/SPEC03.md §4.2, see below) in the mandated order: **Look up a CVE** (default), **Paste a Vector**, **Major CVEs**. The page description was rewritten for a non-technical reader and gained a `MoreExpander` with plain-external links to NVD's and MITRE's CVE search (no network call from the app itself — just `<a href>`s).
- **Offline detection (§6.4)**: `web/src/lib/online.ts`'s `useOnlineStatus()` hook wraps `navigator.onLine` plus `online`/`offline` window event listeners, returning a setter too — a CVE lookup that fails with a `502` (NVD unreachable, per `nvd.ts`'s `fetchNvdCve`/`fetchNvdCveSearch`) also flips it false, since `navigator.onLine` is unreliable on some platforms/only reflects the network interface, not actual internet reachability. The "Look up a CVE" tab gets `disabled` + the exact-text tooltip whenever offline; "Major CVEs" gets the same treatment only when it *also* has no cached data to fall back on (checked client-side against the already-fetched `MajorCvesResponse` in state, not re-derived from the server). An effect auto-switches away from either tab to "Paste a Vector" if the user goes offline while on them — same redirect-and-focus behavior v1 already had for a failed CVE lookup (the existing focus-on-mode-change effect on `vectorInputRef` covers the focus part unchanged).
- **CVE lookup errors (§6.3)**: `WarningBanner.tsx` (new) renders any `cveError` as a red-accented `.callout-warning` card with a warning icon instead of bare `.error-text`. Only a `502` (unreachable) redirects to the paste tab, per §6.3's "keeps v1's behavior" carve-out — a 404 (not found) or 422 (no CVSS data) now stays on the CVE tab with the banner explaining why, since those aren't connectivity problems and redirecting away would be unhelpful.
- **Major CVEs tab (§6.5, new feature)**: fetched via `getMajorCves()` (`api.ts`) on mount and rendered as a plain list (`.major-cve-row`) of ID + outline `SeverityPill` + score + published date, sourced from the new server endpoint (see "NVD lookup & saved vulnerabilities" above for the merge/cache logic). A `"Last updated <timestamp>"` / `"Updated just now"` line reflects the response's `cached`/`fetchedAt`. Clicking a row calls the same `performCveLookup()` used by manual CVE-ID entry and switches to the CVE tab — "exactly as if they had searched that CVE ID," per spec, not a separate code path.
- **Overwrite messaging (§6.6)**: `ScorePage.tsx` fetches `listVulnerabilities()` on mount (refreshed again after every successful save) and locally matches the current result against that list — by `cveId` for a CVE-sourced lookup, by `result.base.vector` (already server-normalized) for a pasted vector — to compute an `overwriteLabel` passed into `ScoreResult`'s save panel. This has to be known *before* the save call resolves (the server's `overwritten` response flag alone is too late — §6.6 requires the message at/before confirmation), so it's derived from client-side state that mirrors what the upsert will match against, not from the save response.

The results screen's signature interaction: each environment's score animates from the base score to its modified score on load (`AnimatedScore`), with color morphing through the severity scale in transit — the "a 10 might actually be a zero" moment, dramatized once. Respects `prefers-reduced-motion`. `None` severity deliberately renders with no color at all (quiet muted text, no pill) rather than a fifth hue — see "Frontend design" below for why. The severity pill next to the animated number derives its severity from the same live (in-transit) value as the number's color, not the final value — both must land together when the animation settles, or the pill spoils the reveal by snapping to its end state early.

Each changed-metric line in the "why" panel (`ScoreChanges.tsx`) shows a colored ▲/▼/– glyph plus a real signed score number (e.g. `-1.3`, `±0.0`) sourced from the server's `ScoreChange.impact`/`direction` fields, plus a closing "Total:" line and a small order-dependency caveat. Exact per-metric numeric attribution isn't inherently well-defined for CVSS (the formula isn't additive), so `impact` is a **sequential/waterfall** figure: `applyEnvironment()` applies each change one at a time and measures the score movement at each step, which means the per-line numbers always telescope exactly to the row's total delta (reassuring when a user asks "how did three items add up to +1.6?") even though the *split* between two interacting changes could differ if the application order changed — see `server/src/scoring/index.ts`. `direction` is simply derived from `impact`'s sign (`worse`/`better`/`neutral`), not a separate ordinal judgment. The severity pill (in both `AnimatedScore` and the base-score-card) sits in a fixed-width `.severity-slot` sized to the longest severity word ("CRITICAL") so score figures stay aligned in a column down a result list regardless of which severity word appears per row — `.pill-none`'s chrome-less rendering still fits the same slot.

#### SPEC03 UI polish, tabs, and CVE detail enrichment (docs/SPEC03.md v1.2)

- **Copy/branding (§2)**: every user-facing "localscore" mention is wrapped in `<strong>`; em-dashes are gone from all web copy, `server/src/catalog/catalog.ts` (question/label/`whyWeAsk`/`finePrint`/`helpDetail`), and user-facing server error strings (`server/src/scoring/parse.ts`, `server/src/lib/nvd.ts`, `server/src/routes/cve.ts`) — rewritten sentence-by-sentence, not hyphen-substituted. `server/test/catalog.test.ts` has a regression test scanning every catalog string field for `—`. `ScoreResult.tsx`'s save-panel trigger is now `.button-save` (green), matching every other Save control.
- **Severity pill legibility (§3)**: `--severity-*-text` (outline pill tokens) were re-darkened for the light theme (previously ~4.4:1 against `--surface`, now ≥4.86:1 against both `--surface` and `--surface-card`); `.badge-severity` bumped to `font-weight: 700`/1.5px border. `--severity-medium`/`--severity-high` (solid pill fills) are now theme-scoped rather than a single value shared by both themes — light gets deepened hues (`#e0a000`/`#d66b3f`), dark keeps the original brighter ones (defined in all three theme token blocks per the file's usual pattern). Contrast values computed via the same WCAG-math procedure the dataviz skill's `validate_palette.js` uses (documented inline in `styles.css`).
- **Primary buttons/tabs (§4)**: new `--button-contrast-bg`/`--button-contrast-ink` tokens (theme-scoped in all three token blocks) implement SPEC02 §2.1's inversion rule for real — `.button-primary` now uses them instead of `--accent`. The Scoring page's three modes are real tabs (`.tabs-shell`/`.tabs`/`.tab`, replacing `.mode-toggle` which has no remaining users and was deleted): a bar visually attached to the panel below it (`.tab-panel`), selected tab rendered as a solid theme-inversion fill. Each panel opens with a `.tab-purpose` line stating what that tab is for. **Gotcha hit here**: `.tab:hover:not(:disabled)` (specificity 0,3,0) was originally unscoped and beat `.tab.is-active` (0,2,0), so hovering the *active* tab silently flipped its text to the same color as its background (invisible) — fixed by scoping the hover rule to `:not(.is-active)`. Caught via Playwright + `getComputedStyle`, not visually (a static screenshot of the non-hovered state looked fine); worth remembering that a hover-state screenshot or a computed-style check is needed to catch this class of bug, a plain screenshot isn't enough.
- **Environments page (§5)**: `.new-environment-form` now has `gap: var(--space-5)` (previously undefined, so the create form's fields touched). The "ready" badge (`.badge.is-complete`) is now the outline tag pattern (transparent fill, colored border/text) using a new `--action-green-text` token (theme-scoped, kept separate from the reserved severity palette) rather than a translucent green fill.
- **Question-help modal (§6)**: `Modal.tsx` takes an optional `className`; `QuestionHelpModal` passes `modal-fixed-height` so `.modal-body` gets a `min-height` (fits the largest of the 12 questions' content, verified via Playwright across all of them) and the dialog no longer visibly resizes between questions. `.modal-header h2` is now `--font-body`/weight 700 (was inheriting the mono display font, scoped to modals only — page headings elsewhere keep IBM Plex Mono). `Question` gained an optional `helpDetail?: string[]` (`server/src/catalog/types.ts`, mirrored in `web/src/types.ts`), carried automatically through `GET /api/catalog`; all 12 questions have 2–4 sentences of plain-English elaboration. Render order in the modal: `whyWeAsk` → `helpDetail` paragraphs → `finePrint`.
- **CVE detail enrichment (§7.4, new feature)**: `extractCveDetails()` (`server/src/lib/nvd.ts`, sibling to `extractVectorOptions`) derives `{ description, published, lastModified, references[], affectedProducts }` from an already-cached `nvd_json` blob at read time — no migration, no new stored columns. `description` picks the `lang: "en"` entry (falls back to the first). `references` preserve NVD's tags and are capped at 20, with `Patch`/`Vendor Advisory`-tagged entries sorted first. `affectedProducts` is a best-effort `vendor product` pair parsed from each CPE 2.3 URI's 4th/5th colon-separated fields, deduped, capped at 15 with a `+N more` remainder count — explicitly *not* a version-accurate applicability check (SPEC03 §11), just orientation. Every field tolerates malformed/absent input rather than throwing (`server/test/nvd-details.test.ts`). `GET /api/cve/:cveId` and `GET /api/vulnerabilities/:id` both gained a `details` field (`null` for a pasted-vector save, since those have no `nvd_json`). `web/src/components/CveDetailsView.tsx` is the one shared rendering component for both the CVE-lookup flow (`ScorePage.tsx`) and the saved-vulnerability detail view (`SavedVulnerabilitiesPage.tsx`) — same component either way, so a saved CVE shows identical context to a fresh lookup, per the spec's "if the user saves the CVE, the additional information should be saved as well" framing (it's free: the same `nvd_json` the §7.1/§7.3-era upsert already carries onto a saved row).
- **Major CVEs / CVE lookup layout (§7.1–§7.3)**: `.major-cve-row` no longer sets mono on the whole row — only `.major-cve-id` and the score figure stay mono, the date and other prose use the body font. Each row now also renders a plain external NVD link (`Icon name="external-link"`, new in `Icon.tsx` alongside `eye`/`eye-off`) with its own `stopPropagation` so it doesn't also trigger the row's in-app lookup — this required splitting the row from one `<button>` wrapping everything into a `<button className="major-cve-row-lookup">` plus a sibling `<a>`, since nested interactive elements aren't valid HTML/ARIA. `.cve-lookup-result` gets `width: 100%` (it was shrink-wrapping to content width due to `.score-form`'s `align-items: flex-start`).
- **Inline more-vulnerable alert (§7.5)**: `EnvironmentResultRow.tsx`'s collapsed summary now renders a red warning-triangle icon immediately before the animated score figure whenever `result.delta > 0`, sized to the score figure's line height (`--text-2xl`) via `.more-vulnerable-icon`. `Icon.tsx` gained an optional `aria-label` prop (when set, flips the SVG from `aria-hidden` to `role="img"` + a `<title>`) since this icon needs to be announced, unlike every other decorative icon use in the app.
- **Saved page (§8)**: the text View/Hide button is now a neutral 2.25rem `.icon-button-quiet` with `eye`/`eye-off` icons (no semantic color — view/hide isn't one of SPEC02 §2.2's four reserved actions) and no more "Loading…" label swap (the button just stays put while the detail fetch is in flight). `.vulnerability-row-header .environment-name` gets `overflow-wrap: anywhere` so a pasted-vector save's default vector-string label (150+ chars, unbroken) wraps inside the card instead of overflowing; `.environment-row-main` already had `min-width: 0` from the environments-list pattern it shares. The edit-mode form is wrapped in `.vulnerability-row-edit` (adds back `var(--space-5)` padding) rather than removing `.vulnerability-row`'s `padding: 0`, which the view-mode header/detail layout still depends on.

#### SPEC04 color refresh and Title Case (docs/SPEC04.md v1.3, §2–§3)

- **Neutral buttons/badges go accent (§2.1)**: `.button` and `.badge` moved from the gray-border/gray-fill treatment to an accent-outline pattern (`--accent` border + text, transparent background, `--accent-wash` hover fill for `.button`) — `--accent` already cleared 4.5:1+ as border/text against both `--surface` and `--surface-card` in both themes (6.32–6.75:1 range, per the dataviz `contrast()` procedure), so no new text-safe token was needed. `.button-primary`/`.button-save`/`.button-cancel`/`.badge.is-complete` are unaffected (they already override fill+color and come later in the cascade). `.button-quiet`, `.link-button`, and `.icon-button-quiet` deliberately stay neutral per the spec's "quiet controls stay quiet" note.
- **Active tab goes accent (§2.2)**: `.tab.is-active` moved off the theme-inversion tokens (`--button-contrast-bg`/`--button-contrast-ink`) onto a solid `--accent` fill + `--accent-ink` text, in both themes — a documented amendment to SPEC02 §2.1/SPEC03 §4.2's inversion rule for tabs specifically; `.button-primary` is unchanged. The SPEC03-era hover gotcha (`.tab:hover` needing `:not(.is-active)` scoping) was already fixed and stayed fixed; re-verified via a `getComputedStyle` check before/after hovering the active tab in both themes.
- **Edit control goes solid yellow (§2.3)**: `.icon-button-edit` moved from the yellow-outline treatment (`--action-yellow-text`, muddy brown in light theme at icon size) to the same solid `--action-yellow` fill + `--action-ink` icon pairing `.button-cancel` uses, in both themes. `--action-yellow-text` had zero remaining consumers after this change and was removed from all three theme token blocks.
- **Pill text goes white-family in light theme (§2.4)**: `--severity-ink` is now theme-scoped (`#ffffff` light, `#0b0b0b` dark, unchanged) instead of a single global value. Since white fails contrast on the old bright light-theme fills, `--severity-low/medium/high/critical` are deepened in the light `:root` and `:root[data-theme="light"]` blocks (hue preserved, each re-derived until it clears ≥4.5:1 against white per the dataviz `contrast()` procedure: low 4.74:1, medium 4.62:1, high 4.68:1, critical 4.94:1); the dark-mode blocks now set all four fills and `--severity-ink` explicitly (previously some fell through to the shared `:root` default, which no longer holds a dark-safe value). `AnimatedScore.tsx`'s `colorFor()` switched from the raw fill tokens to `--severity-*-text` for its score-number foreground color — the deepened light `medium` fill measured just under 4.5:1 (4.39:1) as raw text against `--surface`, and the dark `critical` fill was already under 4.5:1 there too (a pre-existing gap); the `-text` tokens clear contrast in both themes and were already validated for exactly this text-on-surface use.
- **Title Case sweep (§3)**: every button/tab/`.link-button` label audited against the spec's list (`EnvironmentsPage.tsx`, `EnvironmentEditPage.tsx`, `ScorePage.tsx`, `InterviewPage.tsx`, `CveDetailsView.tsx`, `EnvironmentResultRow.tsx`) — option cards, `aria-label`s, tooltips, and placeholders were left in sentence case per the spec's exemptions.

#### SPEC04 environment risk warning (docs/SPEC04.md v1.3, §4)

- **`server/src/scoring/raising.ts`** (new) encodes the §4.1 rule as metric+value pairs per CVSS version, not question IDs, so catalog evolution keeps working: `4.0` flags `CR/IR/AR = H`, `MSC = H`, `MSI`/`MSA` ∈ `{H, S}`; `3.1` flags `CR/IR/AR = H` and `MS = C`. Only `override` effects are ever checked (`cap` never raises severity by definition, SPEC01 §2.2). `computeRaisesScores()` returns the per-version booleans; `computeRaisingAnswers()` returns the deduplicated `{ questionId, optionId }` provenance (a single answer like Q8's stepping-stone option can produce three raising metrics at once but should only be listed once).
- **`GET /api/environments`** (list) and **`GET /api/environments/:id`** (detail) both gained `raisesScores: { "4.0": boolean, "3.1": boolean }`; detail additionally gained `raisingAnswers`. Both are computed by `deriveMetrics()` on `environment_answers` at request time (the same freshness approach `POST /api/score` already uses) rather than read from the persisted `environment_metrics` cache, since `DerivedMetric` carries the questionId/optionId provenance the cache doesn't. An environment with no answers reports `false`/`false` and an empty array. See `API.md`.
- **`EnvironmentsPage.tsx`** shows a red `Icon name="warning"` (`.raises-scores-icon`, same `--action-red-text` glyph/color grammar as `EnvironmentResultRow.tsx`'s per-result icon) in the row's badge row whenever `raisesScores` is true for either version. It's not a separate interactive control — the user reaches the explanation through the row's existing Edit affordance, per the spec's "icon → curiosity → Edit → explanatory callout" framing.
- **`EnvironmentEditPage.tsx`** now also fetches the catalog (`getCatalog()`) to translate `raisingAnswers` provenance into plain English, and renders a `.callout-warning` (new `.raises-scores-list` for the per-answer lines) above the name/description card whenever the flag is set: states plainly that scores can go higher here, lists each responsible question + chosen answer text, and recommends review by a security professional. It's purely informational — doesn't block editing or the interview — and disappears on the next fetch once re-answering no longer triggers the rule (verified via Playwright: re-answering Q8 from "stepping stone" to "a dead end" removes the callout without a reload).
- Tests: `server/test/environments-route.test.ts`'s new `raisesScores` describe block covers Q8 stepping-stone (both versions), Q9 safety=yes (4.0 only), Q5/Q6/Q7 Catastrophic (both versions), the worked-example Disposable Dev Lab answers (not flagged), an empty environment (not flagged), and supplemental-only Q10–Q12 answers (not flagged) — including the detail route's `raisingAnswers` provenance and that the list route omits it. `server/test/score-route.test.ts` adds a consistency cross-check tying a flagged profile to a real `POST /api/score` producing `delta > 0`, not just the lookup table in isolation.
#### SPEC04 NVD-description prefill and collapsed CVE details (docs/SPEC04.md v1.3, §5)

- **NVD-description prefill (§5.1)**: `POST /api/vulnerabilities` (`server/src/routes/vulnerabilities.ts`) now prefills an NVD-sourced save's (`cveId` set) `description` from `extractCveDetails()`'s English description, using the row's effective `nvd_json` *after* the existing COALESCE carry-through — so the normal lookup-then-save path (which always has `nvd_json` cached) picks it up with no client change. Only fires when `description` is otherwise empty (never overwrites a non-empty one, including one the user cleared, edited, or a prior prefill), and only for `cveId` saves — a pasted-vector save stays empty. No migration, no new column; `vulnerabilities.description` already existed (SPEC02 §7.2). Tests in `server/test/vulnerabilities-route.test.ts`'s new "NVD-description prefill" block cover the prefill itself, that a `PUT`-edited description survives a re-save, that pasted-vector saves stay empty, and that a missing/no-description NVD payload saves cleanly (never a 500).
- **Collapsed CVE details (§5.2)**: `CveDetailsView.tsx` now renders a summary header button (`.cve-details-summary`, real `<button aria-expanded>`, the same `.disclosure`/`.is-open` rotating-chevron grammar `EnvironmentResultRow.tsx` uses) reading "CVE Details · Published <date>" — collapsed by default on every fresh render (no open-state persistence), expanding on click to reveal the description/dates/affected-products/references block exactly as before, now wrapped in `.cve-details-body`. Since the component is shared between the Scoring page's fresh-lookup flow and the Saved page's expanded-row detail view, one change covers both surfaces. The internal description "Show More"/"Show Less" clamp is unchanged, just now nested inside the expanded state.

#### SPEC05 global chrome (docs/SPEC05.md v1.4, §2)

- **Theme toggle becomes a switch (§2.1)**: `ThemeToggle.tsx` now renders a real `<button role="switch" aria-checked>` (`.theme-switch`) instead of a quiet icon button — a pill track with a sliding thumb, light = off (thumb left, neutral `--surface-sunken` track), dark = on (thumb right, `--accent`-filled track). The active icon rides inside the thumb at 18px (up from the old button's 16px default), the other allowed convention (both icons static in the track, thumb covering one) was passed over as cluttered at this size. `prefers-reduced-motion` is already handled by the app-wide `*`-scoped rule in `styles.css`, so no separate override was needed. Contrast (WCAG procedure): thumb icon (`--ink`) against the thumb (`--surface-card`) is 19.17:1 light / 17.42:1 dark (near-black-on-near-white, trivially clears); the thumb against the accent track background is 6.49:1 light / 6.05:1 dark, recorded in a `styles.css` comment alongside the rule.
- **Edit control redesign (§2.2, amends SPEC02 §2.2 / SPEC04 §2.3)**: `.icon-button-edit` moves off SPEC04's solid `--action-yellow` fill (which read as a glaring square dominating every row) onto the same outline box model `.icon-button-delete` already uses, just in the `--accent` family instead of red — quiet until sought out, but reads as a clear peer of Delete rather than the loudest thing on the card. This releases SPEC02 §2.2's "Edit and Cancel share the yellow family" requirement for Edit specifically (`.button-cancel` is unchanged, still solid yellow); `--accent` was already contrast-validated as border/text against both `--surface` and `--surface-card` in both themes (SPEC04 §2.1, 6.32–6.75:1), so no new token was needed and no `--action-*` token was left orphaned. Verified in both themes on the Environments list, the environment edit view, and the Saved list (all three consume the one shared class).
- **Layout-stable scrollbar (§2.3)**: `html { overflow-y: scroll; }` reserves scrollbar space unconditionally, so navigating between a short page (e.g. an empty Saved list) and a long one (e.g. Environments with several rows) no longer shifts centered content horizontally. Verified via Playwright: the `.topbar-inner` header's computed `x`/`width` are identical between the two pages in both themes.

#### SPEC05 location field and two-tier risk warning (docs/SPEC05.md v1.4, §3)

- **Location field (§3.1)**: migration `0004_environments_location.sql` adds `environments.location TEXT NOT NULL DEFAULT ''` — a short, optional, single-line, plain-text place label ("us-east-1", "Building 4, rack 12"), deliberately not markdown and not involved in scoring/derivation/the interview. `POST`/`PUT /api/environments` accept an optional `location` (same COALESCE-on-missing-field pattern `PUT` already used for `description`), trimmed server-side; both `GET` routes return it. `EnvironmentsPage.tsx`'s create form and `EnvironmentEditPage.tsx` both gained a `.input` for it; a non-empty location renders as a small muted `.environment-location` line (new token-driven class: `--text-sm`/`--ink-muted`) directly under the environment name, above the markdown description — empty renders nothing.
- **Risk warning becomes two-tier (§3.2), redesigning docs/SPEC04.md §4**: the SPEC04-era rule flagging `CR/IR/AR = H` (Q5/Q6/Q7 "Catastrophic") over-triggered — an environment that legitimately has a lot to lose (a government IL6 system) was flagged permanently, making the flag meaningless exactly where stakes are highest. Per explicit user sign-off (SPEC04 §8's pre-registered fallback, now exercised), the warning splits:
  - **Tier 1 (`raisesScores`, narrowed)**: `server/src/scoring/raising.ts`'s `RAISING_VALUES` drops `CR`/`IR`/`AR` entirely — the raising set is now exactly `MSC/MSI/MSA = H` (4.0 Q8 stepping-stone), `MS = C` (3.1 Q8), `MSI/MSA = S` (4.0 Q9 safety=yes). API shape (`raisesScores`, `raisingAnswers`) is unchanged, only the trigger set narrows. Accepted trade-off, documented in `raising.ts`: a `CR/IR/AR`-only profile can still produce `delta > 0` on the scoring page without carrying this flag — stakes alone aren't a misconfiguration, which is exactly what Tier 2 exists to catch in combination with readiness answers.
  - **Tier 2 (`redFlags`, new)**: `server/src/scoring/redflags.ts` computes flags straight from `environment_answers` (not derived metrics, since Q10–Q12's supplemental answers never produce a metric), deliberately catalog-coupled (conditions reference `questionId`/`optionId` pairs directly, defended by a catalog-integrity test in `environments-route.test.ts` rather than indirection). "High stakes" = any of Q5/Q6/Q7 "Catastrophic" or Q9 safety "yes"; every rule pairs a stakes condition with a readiness gap so a flag never fires on stakes alone: `uncertain_recovery` (high stakes + Q10 "Uncertain"), `concentrated_availability` (Q7 "immediate" + Q11 "concentrated"), `hard_to_patch` (high stakes + Q12 "Hard"). Skipped/unanswered questions never satisfy a condition. `GET /api/environments` (list) returns `redFlags: string[]` (just ids); `GET /api/environments/:id` (detail) returns `redFlags: { id, answers[] }[]` with the same deduplicated-provenance shape `raisingAnswers` already uses.
  - **UI**: `EnvironmentsPage.tsx`'s row warning triangle now shows when `raisesScores` is true for either version **or** `redFlags` is non-empty (generalized `aria-label`, still not interactive — Edit remains the path to the explanation). `EnvironmentEditPage.tsx`'s `.callout-warning` splits into up to two `.callout-section` blocks (score-raising, then red flags), separated by a hairline divider (`.callout-section + .callout-section`) only when both are present; either renders alone when only one tier triggers, and the whole callout disappears when neither does. Each red flag renders its plain-English meaning (`RED_FLAG_COPY` in `EnvironmentEditPage.tsx`) followed by its responsible question/answer pairs resolved via the catalog, reusing the existing `.raises-scores-list` treatment.
- Tests: `server/test/environments-route.test.ts`'s `raisesScores` describe block now asserts a Catastrophic-only profile does **not** flag (inverted from SPEC04), while Q8/Q9 cases and the `POST /api/score` consistency cross-check (`score-route.test.ts`) stay unchanged. A new `redFlags` describe block covers each flag's exact trigger condition, that a condition-half alone never fires, that Q9 safety=yes also satisfies "high stakes," skipped answers, an empty environment, and multiple simultaneous flags; a separate `describe` asserts every `questionId`/`optionId` referenced by `redflags.ts`'s rule set (exported as `REFERENCED_ANSWERS`) exists in the shipped catalog. `server/test/migrations.test.ts` gained a migration-0004 test following the existing pre-existing-row pattern.

#### SPEC05 Scoring-page focus and typography (docs/SPEC05.md v1.4, §4)

- **Tab inputs take focus symmetrically (§4.1)**: `ScorePage.tsx` had a paste-only special case (an effect that only focused `vectorInputRef` on `mode === "paste"`). It's now one effect keyed on `mode` that also focuses a new `cveInputRef` (wired to `#cve-input`) whenever `mode === "cve"` — including on the initial render, since "Look up a CVE" is the default tab. The Major CVEs tab has no input and is untouched. The existing offline-redirect effect (which switches away from the CVE/Major-CVEs tabs when offline) is unchanged and still runs first when applicable, so the focus effect only ever tries to focus a tab that's actually showing.
- **Major-CVE ID typography (§4.2)**: `.major-cve-id` had no explicit `font-size` (it inherited the ambient body size, sitting oddly small next to the score figure). Set to `var(--text-lg)` (1.25rem) — verified via Playwright to read as the row's primary identifier at both desktop and the 480px stacked layout, with no overflow or wrapping regressions.

#### SPEC05 Saved-page fixes (docs/SPEC05.md v1.4, §5)

- **Header actions stop wrapping (§5.1)**: `.vulnerability-row-header` moved from `flex-wrap: wrap` to `align-items: flex-start` + `flex-wrap: nowrap` (with a `@media (max-width: 480px)` override restoring wrap for a deliberate stacked layout at that width) — previously, once a row's description made `.environment-row-main` tall/wide enough, the eye/edit/delete action cluster would wrap below it instead of staying pinned top-right. `.environment-row-main`'s existing `min-width: 0` and the row's existing `overflow-wrap: anywhere` (SPEC03 §8.3, for maximal unbroken pasted-vector labels) and `.environment-row-actions`'s existing `flex-shrink: 0` needed no changes — the wrapping was purely `.vulnerability-row-header`'s own `flex-wrap`. Verified via Playwright: the actions column's computed `x`/`width` are identical across a plain row, a row with a multi-line markdown description, and a row whose label is a 150+ char unbroken CVSS v4.0 vector, at desktop and both themes.
- **CVE-details disclosure adopts the row grammar (§5.2)**: `.cve-details-summary` didn't own a dedicated card the way `.result-row-summary` does (it's a header inside someone else's padding — either the Scoring page's lookup-result card or the Saved page's expanded-row panel) so rather than copying that pattern verbatim, it gets its own bounded row instead: `border` + `border-radius: var(--radius-md)` + `background: var(--surface-sunken)` (darkening slightly to `--surface-card` on hover), replacing the old unpadded-text-button-with-a-16px-chevron treatment that didn't read as clickable. Chevron bumped from `size={16}` to `size={20}`, matching `.result-row-summary`'s. Collapsed-by-default behavior, `aria-expanded`, and the internal description clamp are all unchanged.
- **De-blued expanded details (§5.3, narrow amendment to SPEC04 §2.1)**: inside `.cve-details-body`, the affected-product chips and reference tags were accent-outline `.badge`s sitting directly beside accent-colored reference links, reading as one undifferentiated wall of blue. Scoped override `.cve-details-body .badge` recolors to `--ink-secondary` (not `--ink-muted`, which only clears 3.41:1/3.50:1 against `--surface`/`--surface-card` in light theme at this size — `--ink-secondary` clears 7.53:1+ in both themes, verified via the dataviz `contrast()` procedure) so the hierarchy reads link = accent = interactive, chip = neutral = metadata. SPEC04 §2.1's "all neutral badges go accent" rule is otherwise unchanged everywhere else (Saved-row source/CVE-ID tags, "no profile yet").
- **NVD link in CVE details (§5.4, new)**: `CveDetailsView` gained an optional `cveId?: string | null` prop (passed from `ScorePage.tsx`'s `cveLookup.cveId` and `SavedVulnerabilitiesPage.tsx`'s `viewing.detail.cveId`) and renders a plain external link to `https://nvd.nist.gov/vuln/detail/<CVE-ID>` (`Icon name="external-link"`, same grammar as the Major-CVE rows' link, SPEC03 §7.2) at the top of the expanded `.cve-details-body` when present. Placed inside the expanded body rather than beside the summary's chevron specifically to avoid nesting an `<a>` inside the `.cve-details-summary` `<button>` (invalid HTML/ARIA) — this also means it needs no `stopPropagation`, unlike the Major-CVE row's version which does nest a sibling link next to a button. No link renders when `cveId` is absent (a pasted-vector save's `details` is `null` anyway, so this only defends against odd states).

#### SPEC06 environment quote-stripping and risk-callout readability (docs/SPEC06.md v1.5, §2)

- **Quote stripping (§2.1)**: `server/src/lib/strings.ts`'s `stripWrappingQuotes()` removes one matched pair of wrapping quotes (straight or curly, double or single) after trimming, repeating until no wrapping pair remains, so `"'My Lab'"` fully unwraps to `My Lab`. Applied to `name` and `location` on both `POST /api/environments` and `PUT /api/environments/:id` (not `description` — markdown legitimately uses quotes). A name that becomes empty after stripping still 400s, same as an empty submission today. The create/edit forms' placeholders no longer model quoted examples (`e.g. My Data Center`, `e.g. us-east-1 or Building 4, rack 12`) since that's what taught users to type them in the first place; the server-side strip is the backstop for API callers and anyone who quotes anyway.
- **Risk-callout readability (§2.2)**: pure presentation change in `EnvironmentEditPage.tsx`/`styles.css`, no change to the callout's trigger logic or copy. Each `.callout-section`'s title/explanatory paragraph is now wrapped in a `.callout-section-intro` div, visually distinct from the `.raises-scores-list` that follows (now indented via `padding-left: var(--space-4)`). Within the list, each item renders as two lines instead of one: a bold `Q:` prefix + question text, then a normal-weight `A:` prefix + answer text (`.raises-scores-question`/`.raises-scores-answer`). Inter-pair spacing widened from `var(--space-1)` to `var(--space-3)` so adjacent question/answer pairs read as distinct units — verified via Playwright against a flag with multi-answer provenance (`uncertain_recovery`, spanning Q5 Catastrophic + Q10 Uncertain) and the multi-answer Q8 stepping-stone case, both themes. Both the score-raising section and every red-flag block share the one `.raises-scores-list` class, so the treatment applies uniformly without forking it.

### Scoring engine

Built on `ae-cvss-calculator` (metaeffekt, Apache-2.0, zero runtime deps, covers CVSS 2.0/3.0/3.1/4.0) after evaluating it per docs/SPEC01.md §2.4 — reproduces the spec's worked example (base `9.8` → environmental `0.0`) and known reference vectors exactly; see `server/test/scoring.test.ts`. It's a CommonJS package under `"type": "module"` — import it with `import pkg from "ae-cvss-calculator"; const { Cvss3P1, Cvss4P0 } = pkg;`, not named imports (TS's `NodeNext` resolution will reject named imports from a CJS package).

- `server/src/scoring/parse.ts` — version detection/routing (§2.5) and friendly parse errors. Note: `AV:X` is *not* an invalid value — `X` is the real "Not Defined" enum member shared by every metric — so it's caught separately as "missing required base metrics" via `isBaseFullyDefined()`, not as an unknown-value error.
- `server/src/scoring/orderings.ts` — the §2.2 cap severity orderings (only the exploitability metrics that ever carry a `cap` effect: AV/AC/AT/PR/UI), and `baseCounterpartMetric()` (strip a leading `M` — works uniformly for every M-prefixed metric, cap or override; CR/IR/AR and the v4 supplemental metrics have no base counterpart). An earlier version of this file also carried a symbolic `severityDirection()` covering every scoring-relevant metric to power the "why" panel's ▲/▼ indicators — removed in favor of `applyEnvironment()` computing the real score impact directly (see below), which is strictly more accurate and needed no CR/IR/AR-style special-casing.
- `server/src/scoring/index.ts` — `applyEnvironment()` (mutates a parsed vector per override/cap rules; also tracks a running score across the mutation loop so each returned `AppliedChange` carries a real `impact` — the score movement caused by that change on top of everything already applied before it — and a `direction` derived from `impact`'s sign) and `scoreForEnvironment()` (parse + apply + compute in one call).
- `server/src/catalog/derive.ts` — `DerivedMetric` now carries `questionId`/`optionId` provenance (used for change explanations), in addition to what's persisted to `environment_metrics`.
- `POST /api/score` recomputes each environment's derived metrics fresh from `environment_answers` at request time rather than reading the persisted `environment_metrics` cache — simpler and self-consistent, at negligible cost (12 questions).
- `Question` (`server/src/catalog/types.ts`) has an optional `finePrint` field for the docs/SPEC01.md §5.2-mandated "what this maps to" disclosures on Q1 (reachability→MAV:A is an approximation, not literal CVSS Adjacent) and Q4 (CVSS has no "interaction impossible" value, so the cap only reduces to the hardest interaction level). `InterviewPage.tsx` surfaces it via the question-help modal (§8.4) rather than inline, so it stays out of the way for non-expert users until they ask for it. `server/test/environments-route.test.ts` covers environment CRUD, resumable partial-answer saves, and metric re-derivation-on-save (§10.6) — previously only exercised incidentally as fixture setup inside `score-route.test.ts`.

### Frontend design

When designing or building UI in `web/` — new screens, layout/visual changes, or anything touching typography/color/spacing — invoke the `frontend-design` skill first. It's installed and available in this environment (verified 2026-07-08). Use it before writing JSX/CSS, not as an afterthought, so the interview and results screens read as intentionally designed rather than default scaffolding.

For any color tied to *data* specifically — severity/status colors, deltas, chart series, stat tiles — also invoke the `dataviz` skill and run its `validate_palette.js` against candidate hexes rather than picking colors by eye. This is how the current severity palette (`--severity-low/medium/high/critical` in `web/src/styles.css`) was derived: the skill's reserved status palette (good/warning/serious/critical), contrast-checked with near-black text on every fill. `None` deliberately has no color at all (quiet muted text) — a content-driven choice, not an oversight, so don't "fix" it by giving it a hue.

### Verifying frontend changes

This environment has no built-in browser/screenshot tool, but a system Chrome install is available at `/Applications/Google Chrome.app`. To actually see and click through UI changes rather than trusting the code: `npm install --no-save playwright-core` (ad-hoc — do **not** add it to any `package.json`, it's a verification tool, not an app dependency), then drive it with `chromium.launch({ executablePath: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", headless: true })`. Screenshot with `page.screenshot({ path, fullPage: true })`.

Pitfall hit while building the interview UI: Playwright's `text=` selector does a case-insensitive substring match, so `page.click("text=Next")` can silently click a non-interactive element whose text happens to contain "next" (e.g. an option description like "...wiped on the **next** automated rebuild") instead of the actual Next button — no error, just a no-op. Use `page.getByRole("button", { name, exact: true })` or scope with a specific CSS class (e.g. `.interview-nav .button-primary`) instead of loose text matching.

### Commands

Run from the repo root unless noted.

- `npm install` — installs all workspaces.
- `npm run dev:server` — server on :8080 with reload (`tsx watch`).
- `npm run dev:web` — Vite dev server with `/api` proxied to :8080 (run alongside `dev:server`).
- `npm run build` — builds `web` (static assets to `web/dist`) then `server` (to `server/dist`, migrations copied alongside).
- `npm start` — runs the built server (`server/dist/index.js`); serves the built frontend + API on one port.
- `npm test` — runs the server's Vitest suite (`server/test/*.test.ts`) then the web's (`web/src/**/*.test.ts`). To run one file: `npm run test --workspace server -- test/catalog.test.ts` (or `--workspace web`).
- `npm run typecheck` / `npm run lint` — both workspaces.
- `podman build --format docker -t localscore .` then `podman run ...` — container build per docs/SPEC01.md §9. The maintainer uses **Podman, not Docker**; the Dockerfile/compose.yaml are plain OCI and must keep working under Docker too, but write any documentation/examples with `podman`. **`--format docker` is required for a direct `podman build`** — Podman's default OCI build format silently drops the Dockerfile's `HEALTHCHECK` instruction with just a warning, no error.
- `podman compose up` — builds and runs via `compose.yaml`. Verified this does **not** need `--format docker`: going through the external `docker-compose` provider already produces a Docker-format image with `HEALTHCHECK` intact (confirmed by `podman inspect` reporting `healthy`). So the flag only matters for a bare `podman build`, not for compose.
- Published image: `docker.io/eengelking/localscore` (tags `latest`, `0.2.0`) — pushed with `podman push`. docs/SPEC01.md §9's example commands reference `ghcr.io/<owner>/localscore`; the maintainer chose Docker Hub instead, so README.md documents `docker.io/eengelking/localscore` as the real, working registry path. See "Publishing a release image" below for the required tag/push/verify procedure — never push an untagged or `latest`-only build.

Verified end-to-end (2026-07-08): both `podman build --format docker` + `podman run`, and `podman compose up`, produce a container `podman inspect` reports as `healthy`, with the API/frontend reachable and a full create-environment round trip working.

`DATA_DIR` (default `./data`) and `PORT` (default `8080`) are read from the environment; see `.env.example`.

### Testing during development vs. before merge

Two distinct testing procedures — don't conflate them:

- **Test 2 (npm, during development)** — the default while iterating. Build and run the built server directly on port 8081 (`PORT=8081`), not the Vite dev server: `npm run build && PORT=8081 node server/dist/index.js`. This matches how the container actually runs (one process, built frontend + API) without the overhead of a full container build on every check. **Always stop the process cleanly when done** (kill it — don't leave it running in the background between tasks). Use this for functional checks, API round trips, and UI verification via Playwright (see "Verifying frontend changes" above) throughout the work.
- **Test 1 (container, last step before merge)** — the final gate confirming the container builds and runs as it will in production, on port 8080. Container name `localscore-test`, image tag `localscore:local`. Before building: check whether a container named `localscore-test` (or anything else) is already bound to port 8080 — if so, **ask the user before taking it down**, don't stop it unilaterally. Always build a fresh image (don't reuse a stale one) — `podman build --format docker -t localscore:local .` — then `podman run --name localscore-test -p 8080:8080 ...` and confirm `/api/health` reports healthy and the frontend loads. Clean up afterward per "Container testing cleanup" below (ask before removing).

### Publishing a release image

This is separate from Test 1 (which only builds `localscore:local` for local verification and is never pushed). A release build is any container image meant to be published to `docker.io/eengelking/localscore`.

1. **Decide the version bump.** Current version lives in the root `package.json` (`server/package.json` and `web/package.json` are kept in sync with it — bump all three together). Default to a **patch or minor bump** from the prior published tag (e.g. `0.1.0` → `0.1.1` or `0.2.0`) for normal releases. Only bump the **major** version (`0.x.y` → `1.0.0`) when the user has explicitly said this is a major/breaking release — never infer "major" on your own from the diff size. If it's ambiguous which bump applies, ask the user rather than guessing.
2. **Update the version** in `package.json`, `server/package.json`, `web/package.json` to the new version number, and update any docs that literally quote the current published tag (README.md's image line, CLAUDE.md's "Published image" line below) so they don't go stale.
3. **Build fresh** — don't reuse a stale local image: `podman build --format docker -t localscore:<new-version> .` (`--format docker` is required for the `HEALTHCHECK` to survive, per the gotcha above).
4. **Tag** the built image for the registry with both the new version and `latest`:
   - `podman tag localscore:<new-version> docker.io/eengelking/localscore:<new-version>`
   - `podman tag localscore:<new-version> docker.io/eengelking/localscore:latest`
5. **Push both tags**:
   - `podman push docker.io/eengelking/localscore:<new-version>`
   - `podman push docker.io/eengelking/localscore:latest`
6. **Verify the push succeeded** — don't just trust a clean exit code from `podman push`. Confirm the new tag is actually live on the registry, e.g. `podman manifest inspect docker.io/eengelking/localscore:<new-version>` (or `skopeo inspect docker://docker.io/eengelking/localscore:<new-version>` if available) and check that `latest` now resolves to the same digest as `<new-version>`.
7. Clean up the local release build per "Container testing cleanup" below (ask before removing) once the push is verified.

### Container testing cleanup

When you `podman run` a container to manually verify something (health check, a route, a full rebuild), it's a temporary test artifact, not something to leave running or lying around:

- **Ask before deleting.** Once testing is done, tell the user what you're about to remove (container name, volume, image) and get a go-ahead before running `podman rm` / `podman rmi` / `podman volume rm` — don't delete silently, even though it's your own test container.
- **Clean up after yourself.** Stop and remove test containers/volumes when done (after confirmation), and run `podman image prune -f` after repeated `podman build` runs against the same tag — each rebuild orphans the previous image as a dangling `<none>` (this happened during initial Podman verification: 4 dangling images, ~1.5 GB, from 3 build iterations of the same `localscore:local` tag).

### Known gotchas

- **npm workspace hoisting affects the Dockerfile.** `npm ci` hoists shared dependencies to the workspace-root `node_modules`, not `server/node_modules` — the runtime image stage copies `/app/node_modules` (root), not a per-workspace one. If you add a server-only dependency that npm decides *not* to hoist (e.g. a conflicting version), the runtime COPY may need to also grab `server/node_modules` — check after `npm ci` whether it exists before assuming it doesn't.
- **`npm audit` reports 5 vulnerabilities** (3 moderate, 1 high, 1 critical) in the `vite`/`vitest`/`esbuild` dev-tooling chain (dev-server-only exposure, not a runtime/production risk). Fixing requires a breaking major-version bump to `vite`/`vitest` — left as-is; don't be surprised by it, and don't `npm audit fix --force` without deliberately taking that upgrade.
- **`podman compose build`/`up` can fail locally** with `error listing credentials - err: exec: "docker-credential-desktop": executable file not found` if `~/.docker/config.json` has `"credsStore": "desktop"` left over from Docker Desktop, even though Podman is what's actually running. Workaround for a one-off command: `DOCKER_CONFIG=<empty-dir-with-{}-config.json> podman compose ...`. Don't edit the user's real `~/.docker/config.json` to fix this — it's outside the project and outside this repo's concern.

## Source of truth

**`docs/SPEC06.md` (v1.5) is the active implementation contract — not yet implemented.** It covers input hygiene (quote-stripping environment names/locations), a readability rework of the environment risk callout, Scoring-page fixes (save-time description, stale-result reset, full-width lookup errors, an unclamped CVE description, a relocated NVD link), and a Saved-page round (bottom-right action cluster, type/severity filters, full-content search). Note SPEC06 amends two SPEC05-era placements (the Saved-row action cluster's top-right pinning, the NVD link's top-of-details position) and removes the CVE description's Show More clamp.

`docs/SPEC01.md` (v1) through `docs/SPEC05.md` (v1.4) are all fully implemented and historical — "docs/SPEC0N.md §N" citations throughout this file and the code are rationale for existing behavior, not a to-do list. Future spec rounds continue the numbering in `docs/` (SPEC07.md, …); when one lands, update this section to point at it as the active contract.

## What this project is

localscore turns a CVSS base score into the score that actually applies to a specific environment. A vulnerability scanner reports a 10.0; CVSS environmental metrics can correct that for systems that aren't worst-case, but the official FIRST calculators are expert-facing. localscore asks ~12 plain-English questions about a "location" (e.g. "My Data Center", "Retail Kiosks") and derives CVSS environmental metric profiles from the answers, then applies those profiles to any pasted CVSS vector or looked-up CVE.

Self-hosted single container, SQLite on a volume, no accounts, no cloud dependency except an optional NVD CVE lookup.

## Mandated tech stack (docs/SPEC01.md §3)

- **Runtime**: Node 22 LTS, TypeScript everywhere, strict mode.
- **Backend**: Hono (Fastify only if Hono proves awkward) serving both the JSON API and the built frontend static assets from one process on one port (default 8080).
- **Frontend**: React + Vite, built to static assets at image build time. Plain CSS (custom properties for theming) or a tiny utility layer — no heavy UI framework, no large-runtime component library. Use the `frontend-design` skill for any UI/visual work — see "Frontend design" under Status.
- **Database**: SQLite via `better-sqlite3`, single file at `/data/localscore.db`, WAL mode. Migrations are sequential numbered SQL files (`migrations/0001_*.sql`, …) applied at startup inside a transaction, tracked in a `schema_migrations` table.
- **Scoring**: MUST match FIRST's reference calculators exactly (v4.0 and v3.1). Use an existing maintained library (`ae-cvss-calculator` is the leading candidate) or vendor FIRST's reference code, validated against reference test vectors before committing. Hand-rolled scoring math without reference-validated test vectors is explicitly not acceptable.
- **Testing**: Vitest. Tests are a release gate (§10).
- **Container**: multi-stage Dockerfile (plain OCI — builds and runs under Docker or Podman), non-root user, `node:22-slim`/alpine runtime, final image < 300 MB, `HEALTHCHECK` on `/api/health`. The maintainer runs Podman day-to-day, so use `podman`/`podman compose` in docs and examples.

No external services, no telemetry. The only outbound network call anywhere in the app is the optional NVD CVE lookup.

## Architecture (why the pieces fit together)

The core design tension the whole spec is built around: **environments store raw interview answers, not derived metrics, as the source of truth.** `environment_answers` (question_id + option_id pairs) is authoritative; `environment_metrics` is a materialized cache re-derived from those answers on every save. This is what lets the question catalog evolve over time (`CATALOG_VERSION`) and lets a completed interview be re-opened with prior answers pre-selected without losing data. Never treat `environment_metrics` as something to hand-edit directly — it's always regenerated from answers + catalog in code.

The **question catalog lives in code** (a versioned TS module), not in the database — see docs/SPEC01.md §5.1 for the `Question`/`Option`/`MetricEffect` shape. Each answer option emits zero or more `MetricEffect`s tagged with an `effect` of `override` or `cap`:

- **`override`**: unconditionally replaces the base metric (used for environment facts like CR/IR/AR, or MVC:N when a location genuinely holds nothing confidential). Can legitimately raise a score above base — this is intentional for the "blast radius" (Q8) and "safety" (Q9) questions.
- **`cap`**: only applies if it makes the metric *less* severe than the base vector's value, using the severity orderings in §2.2. Never raises severity.

Conflict rule when two questions touch the same metric (§5.3): later question's `override` wins; a `cap` never displaces an existing `override`. The concrete case baked into the catalog: Q9 Safety's MSI/MSA override always wins over Q8 blast-radius for those same metrics.

**Supplemental metrics (v4.0 S, R, V, RE)** are collected and stored per-environment but are strictly display-only — they MUST NOT affect any computed score (v4.0 spec §6). **AU, U (provider-assigned) and threat/temporal metrics (E, RL, RC)** are the opposite: never interviewed, per-vulnerability not per-environment, displayed/scored only from what's pasted in.

Scoring flow (§6): parse the pasted vector → detect version (v4.0 / v3.1 / v3.0-scored-as-3.1-with-a-disclosed-note / v2-rejected) → for each environment with a completed interview for that version, apply stored effects (override then cap-if-less-severe) on top of the base vector → compute with version-appropriate reference math → render one row per environment sorted by modified score descending, each expandable into a plain-English "why" panel (question/answer text, not raw metric codes, with codes in fine print).

An environment with zero answers MUST reproduce the base score exactly — this is both a design invariant (§2.3) and a required test (§10.2).

## Domain rules for the scoring code

- CVSS v3.0 vectors are scored using v3.1 equations against the v3.1 profile, with a UI-visible disclosure — never silent (§2.5).
- v2.0 vectors (including bare `AV:N/AC:L/Au:N/...` syntax) get a friendly "not supported yet" rejection, not an attempted parse.
- If a pasted vector already contains environmental metrics, the environment profile's values win for metrics the profile defines; the vector's environmental values fill in the rest. UI should warn when this happens.
- Full metric-to-question mapping is in Appendix A of docs/SPEC01.md — consult it rather than re-deriving which question feeds which metric.
- The worked example in §6 (base `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` = 9.8, "Disposable Dev Lab" profile → 0.0) MUST exist as an exact-match test case.

## API surface (docs/SPEC01.md §8)

All JSON under `/api`, no auth in v1, errors as `{ error: string }` with correct status codes. Frontend is served for all non-`/api` paths (SPA fallback to `index.html`) from the same process/port. See §8 for the full route table (`environments` CRUD + answers, `POST /api/score`, `GET /api/cve/:cveId`, `vulnerabilities` CRUD, `/api/health`, `/api/catalog`).
