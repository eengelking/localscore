# Port the server listens on.
PORT=8080

# Directory holding the SQLite database file (localscore.db). Must be writable.
DATA_DIR=./data

# Optional: raises the NVD API rate limit from ~5/30s to a higher authenticated limit.
# NVD_API_KEY=
